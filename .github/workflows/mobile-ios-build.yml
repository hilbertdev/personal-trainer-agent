# Build and submit iOS app via EAS (Expo Application Services)
#
# Required GitHub secrets:
#   EXPO_TOKEN    from: expo.dev → Account Settings → Access Tokens

name: Mobile iOS Build and Submit

on:
  push:
    branches: [main]
    paths:
      - 'src/mobile/**'
      - '.github/workflows/mobile-ios-build.yml'
  workflow_dispatch:
    inputs:
      profile:
        description: 'EAS build profile'
        required: true
        default: 'production'
        type: choice
        options: [development, preview, production]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/mobile

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/mobile/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Build (production)
        run: eas build --platform ios --profile ${{ github.event.inputs.profile || 'production' }} --non-interactive

      - name: Submit to App Store (production only)
        if: ${{ (github.event.inputs.profile || 'production') == 'production' }}
        run: eas submit --platform ios --latest --non-interactive
